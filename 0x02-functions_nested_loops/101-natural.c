#include <stdio.h>
/**
 * main - prints natural numbers below 1024 that are
 * multiples of 3 or 5
 *
 * Return: Always 0.
 */
int main(void)
{
	int i, z;

	for (i = 1; a < 1024; i++)
{
	if ((i % 3) == 0 || (i % 5) == 0)
	z += i;
}
	printf("%d\n", z);
	return (0);
}
